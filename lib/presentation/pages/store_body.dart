import 'package:flutter/material.dart';

class StoreBody extends StatelessWidget {
  const StoreBody({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Store Body'),
    );
  }
}